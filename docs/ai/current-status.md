# Текущий статус проекта

Проект: Pika

Стадия разработки: ранняя.

---

# Текущий подход разработки

Разработка начинается с **исследования источников**, а не с полной архитектуры.

Причина:

структура данных источников пока неизвестна.

Необходимо сначала понять:

- какие данные доступны
- как устроены страницы
- есть ли API
- какие поля можно получить
- как выглядят episodes и playback options

---

# Sandbox

Для исследования используется отдельный проект:

Pika.Sandbox

Это временный Web API проект.

В нем будут находиться:

- экспериментальные модели
- черновая бизнес-логика
- тестовый парсинг
- HTTP запросы к источникам

Sandbox может содержать код, который **не соответствует финальной архитектуре**.

Его задача — исследование.

---

# Что уже сделано

- создан Git repository
- написан README
- создан базовый Avalonia Desktop проект
- описана предварительная архитектура
- добавлены AI docs
- создан тестовый проект-песочница Pika.Sandbox
- произведено исследование источников
- выполнены базовые HTTP запросы
- сохранены примеры ответов
- создан проект Pika.Sandbox
- создан парсер m3u8 через Playwright
- получен ответ в ввиде json с {url: m3u8}
- протестировано воспроизведение полученного `m3u8` в разных плеерах
- подтверждено, что `mpv` воспроизводит поток корректно
- подтверждено, что `VLC / libVLC` не воспроизводит тот же поток
- подтверждено, что `ffmpeg` может обработать поток
- подтверждено, что проблема находится не в получении `m3u8`, а в выборе playback backend
- создан Pika.Playback.Mpv
- реализован interop MpvNative (LibraryImport)
- реализован MpvCore (lifecycle + команды)
- реализован MpvRenderBridge (render API + OpenGL FBO)
- реализован VideoGlControl в desktop
- подтвержден встроенный playback в окне Avalonia (Linux-first)

---

# Ошибки / ограничения / наблюдения

Ошибки которые я зафиксировал на этапе создания парсера ссылки: 

1. На сайте помимо kodik есть еще другие плееры (как минимум 3 других)
2. Если плеер kodik не выбран по дефолту - ссылка не будет получена
3. `VLC / libVLC` не воспроизводит полученные `m3u8` ссылки
4. `mpv` воспроизводит те же `m3u8` ссылки корректно
5. `ffmpeg` подтверждает валидность потока, значит проблема находится в playback backend, а не в самой ссылке
6. видео может визуально пропадать при неверной разметке контейнера (перекрытие UI)

---

# План разработки (текущий)

Парсер:
1. Продолжить реализацию парсера
2. Реализовать парсинг доступного качества
3. Реализовать парсинг доступных озвучек

Плеер:
1. стабилизация layout поверх видео
2. улучшение диагностики ошибок playback
3. расширение функционала (кнопки, перемотка - нужно добиться вида полноценного плеера)

---

# Текущий Flow

Описан в данном [файле](flow.excalidraw)

Это excalodraw файл, в котором я на примитивных схемах попытлся описать то, как я вижу текущий поток данных в приложении

В нем я выделил 2 основных JSON файла. 

Request JSON:
```json
{
  "url": "ссылка на тайтл и серию"
}
```
Данный JSON отвечает за отправку через API ссылки, по которой будет происходить поиск данных

Response JSON

```json
{
  "sourceUrl": "...",
  "players": [
    {
      "name": "kodik",
      "data": [
        {
          "voice": "AniDUB",
          "quality": "720p",
          "m3u8Url": "...",
          "resolvedAt": "2026-03-11T10:00:00Z"
        }
      ]
    }
  ]
}
```

Это необходимый ответ от API. В нем содержатся вся необходимая информация такая как:
- изначальная ссылка на аниме
- список объектов плеера
  - наименование плеера
  - список данных плеера
    - озвучка
    - качество
    - ссылка потока
    - время получения ссылки (т.к. она временная)

---

# После Sandbox

Когда станет понятно:

- какие сущности реально нужны
- какие данные доступны
- какие структуры используются

будет выполнен рефакторинг:

Sandbox код будет разделен на:

Core / Domain  
Infrastructure  
Connectors  
API  
Worker

---
